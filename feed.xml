<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://www.jpcpereira.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://www.jpcpereira.com/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-10T00:59:44+00:00</updated><id>https://www.jpcpereira.com/feed.xml</id><title type="html">João Pereira</title><subtitle>Personal website of João Pereira, Data Science Lead @ adidas. </subtitle><entry><title type="html">Fine-tune MPT-7B on Amazon SageMaker</title><link href="https://www.jpcpereira.com/blog/2023/finetune-mpt-7b/" rel="alternate" type="text/html" title="Fine-tune MPT-7B on Amazon SageMaker"/><published>2023-06-20T00:00:00+00:00</published><updated>2023-06-20T00:00:00+00:00</updated><id>https://www.jpcpereira.com/blog/2023/finetune-mpt-7b</id><content type="html" xml:base="https://www.jpcpereira.com/blog/2023/finetune-mpt-7b/"><![CDATA[<p>New large language models (LLMs) are being announced every week, each trying to beat its predecessor and take over the evaluation leaderboards. One of the latest models out there is <a href="https://www.databricks.com/blog/mpt-7b">MPT-7B</a> that was released by MosaicML. Unlike other models of its kind, this 7-billion-parameter model is open-source and licensed for commercial use (<a href="https://github.com/mosaicml/llm-foundry/blob/main/LICENSE">Apache 2.0 license</a>) 🚀.</p> <p>Foundation models like <a href="https://www.databricks.com/blog/mpt-7b">MPT-7B</a> are pre-trained on datasets with trillions of tokens (100 tokens ~ 75 words) crawled from the web and, when prompted well, they can produce impressive outputs. However, to truly unlock the value of large language models in real-world applications, smart prompt-engineering might not be enough to make them work for your use case and, therefore, fine-tuning a foundation model on a domain-specific dataset is required.</p> <p>LLMs have billions of parameters and, consequently, fine-tuning such large models is challenging. Good news is that fine-tuning is much cheaper and faster as compared to pre-training the foundation model given that 1) the domain-specific datasets are “small” and 2) fine-tuning requires only a few passes over the training data.</p> <p><strong>Here is what we will learn in this article:</strong></p> <ul> <li>How to create and structure a dataset for fine-tuning a large language model.</li> <li>What is and how to configure a distributed training job with fully sharded data parallel.</li> <li>How to define a 😊 HuggingFace estimator.</li> <li>How to launch a training job in Amazon SageMaker that fine-tunes <a href="https://www.databricks.com/blog/mpt-7b">MPT-7B</a>.</li> </ul> <h2 id="install-dependencies-and-set-s3-paths">Install dependencies and set S3 paths</h2> <p>Let’s start by installing the <a href="https://sagemaker.readthedocs.io/en/stable">SageMaker Python SDK</a> and a few other packages. This SDK makes it possible to train and deploy machine learning models on AWS with a few lines of Python code. The code below is available in the <a href="https://github.com/jpcpereira/sagemaker-fine-tune-mpt-7b/blob/main/sagemaker_finetuning.ipynb"><code class="language-plaintext highlighter-rouge">sagemaker_finetuning.ipynb</code></a> notebook on Github. Run the notebook in SageMaker Studio, a SageMaker notebook instance, or in your laptop after <a href="https://docs.aws.amazon.com/signin/latest/userguide/command-line-sign-in.html">authenticating to an AWS account</a>.</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="err">!</span><span class="n">pip</span> <span class="n">install</span> <span class="sh">"</span><span class="s">sagemaker==2.162.0</span><span class="sh">"</span> <span class="n">s3path</span> <span class="n">boto3</span> <span class="o">--</span><span class="n">quiet</span>

<span class="kn">from</span> <span class="n">sagemaker.huggingface</span> <span class="kn">import</span> <span class="n">HuggingFace</span>
<span class="kn">from</span> <span class="n">sagemaker.inputs</span> <span class="kn">import</span> <span class="n">TrainingInput</span>
<span class="kn">from</span> <span class="n">sagemaker</span> <span class="kn">import</span> <span class="n">s3_utils</span>
<span class="kn">import</span> <span class="n">sagemaker</span>
<span class="kn">import</span> <span class="n">boto3</span>
<span class="kn">import</span> <span class="n">json</span></code></pre></figure> <p>Next step is to define the paths where the data will be saved in S3 and create a SageMaker session.</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="c1"># Define S3 paths
</span>
<span class="n">bucket</span> <span class="o">=</span> <span class="sh">"</span><span class="s">&lt;YOUR-S3-BUCKET&gt;</span><span class="sh">"</span>
<span class="n">training_data_path</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">s3://</span><span class="si">{</span><span class="n">bucket</span><span class="si">}</span><span class="s">/toy_data/train/data.jsonl</span><span class="sh">"</span>
<span class="n">test_data_path</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">s3://</span><span class="si">{</span><span class="n">bucket</span><span class="si">}</span><span class="s">/toy_data/test/data.jsonl</span><span class="sh">"</span>
<span class="n">output_path</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">s3://</span><span class="si">{</span><span class="n">bucket</span><span class="si">}</span><span class="s">/outputs</span><span class="sh">"</span>
<span class="n">code_location</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">s3://</span><span class="si">{</span><span class="n">bucket</span><span class="si">}</span><span class="s">/code</span><span class="sh">"</span>

<span class="c1"># Create SageMaker session
</span>
<span class="n">sagemaker_session</span> <span class="o">=</span> <span class="n">sagemaker</span><span class="p">.</span><span class="nc">Session</span><span class="p">()</span>
<span class="n">region</span> <span class="o">=</span> <span class="n">sagemaker_session</span><span class="p">.</span><span class="n">boto_region_name</span>
<span class="n">role</span> <span class="o">=</span> <span class="n">sagemaker</span><span class="p">.</span><span class="nf">get_execution_role</span><span class="p">()</span></code></pre></figure> <h2 id="build-a-fine-tuning-dataset">Build a fine-tuning dataset</h2> <p>We will create a dummy dataset to demonstrate how to fine-tune MPT-7B. Since training models of this size on a complete dataset takes long and is costly, it is a good idea to first test &amp; debug the training job on a small dataset and second scale training to the complete dataset.</p> <ul> <li><strong>Format dataset as a list of dictionaries</strong> –– The dataset should be formatted as a list of dictionaries, where each example has a key-value structure, <em>e.g.</em>,</li> </ul> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="p">{</span>
<span class="sh">"</span><span class="s">prompt</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">What is a Pastel de Nata?</span><span class="sh">"</span><span class="p">,</span>
<span class="sh">"</span><span class="s">response</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">A Pastel de Nata is a Portuguese egg custard tart pastry, optionally dusted with cinnamon.</span><span class="sh">"</span>
<span class="p">}</span></code></pre></figure> <p>The <code class="language-plaintext highlighter-rouge">prompt</code> is the input given to the model (<em>e.g.</em>, a question). The response is the output that the model is trained to predict (<em>e.g.</em>, the answer to the question in the <code class="language-plaintext highlighter-rouge">prompt</code>). The raw prompt is often preprocessed to fit in a prompt template that helps the model to generate better outputs. Note that the model is trained for causal language modelling, so you can think of it as a “document completer”. It is a good idea to design the prompt template in such a way that the model thinks that it is completing a document. Andrej Karpathy explains well this mechanism in his talk <a href="https://youtu.be/bZQun8Y4L2A?si=_8RcLNPJnBALl3I7">State of GPT</a>.</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="n">prompt_template</span> <span class="o">=</span> <span class="sh">"""</span><span class="s">Write a response that appropriately answers the question below.

### Question:

{question}

### Response:

</span><span class="sh">"""</span>

<span class="n">dataset</span> <span class="o">=</span> <span class="p">[</span>
<span class="p">{</span><span class="sh">"</span><span class="s">prompt</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">What is a Pastel de Nata?</span><span class="sh">"</span><span class="p">,</span>
<span class="sh">"</span><span class="s">response</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">A Pastel de Nata is a Portuguese egg custard tart pastry, optionally dusted with cinnamon.</span><span class="sh">"</span><span class="p">},</span>
<span class="p">{</span><span class="sh">"</span><span class="s">prompt</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Which museums are famous in Amsterdam?</span><span class="sh">"</span><span class="p">,</span>
<span class="sh">"</span><span class="s">response</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Amsterdam is home to various world-famous museums, and no trip to the city is complete without stopping by the Rijksmuseum, Van Gogh Museum, or Stedelijk Museum.</span><span class="sh">"</span><span class="p">},</span>
<span class="p">{</span><span class="sh">"</span><span class="s">prompt</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Where is the European Parliament?</span><span class="sh">"</span><span class="p">,</span>
<span class="sh">"</span><span class="s">response</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Strasbourg is the official seat of the European Parliament.</span><span class="sh">"</span><span class="p">},</span>
<span class="p">{</span><span class="sh">"</span><span class="s">prompt</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">How is the weather in The Netherlands?</span><span class="sh">"</span><span class="p">,</span>
<span class="sh">"</span><span class="s">response</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">The Netherlands is a country that boasts a typical maritime climate with mild summers and cold winters.</span><span class="sh">"</span><span class="p">},</span>
<span class="p">{</span><span class="sh">"</span><span class="s">prompt</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">What are Poffertjes?</span><span class="sh">"</span><span class="p">,</span>
<span class="sh">"</span><span class="s">response</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Poffertjes are a traditional Dutch batter treat. Resembling small, fluffy pancakes, they are made with yeast and buckwheat flour.</span><span class="sh">"</span><span class="p">},</span>
<span class="p">]</span>

<span class="c1"># Format prompt based on template
</span>
<span class="k">for</span> <span class="n">example</span> <span class="ow">in</span> <span class="n">dataset</span><span class="p">:</span>
<span class="n">example</span><span class="p">[</span><span class="sh">"</span><span class="s">prompt</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">prompt_template</span><span class="p">.</span><span class="nf">format</span><span class="p">(</span><span class="n">question</span><span class="o">=</span><span class="n">example</span><span class="p">[</span><span class="sh">"</span><span class="s">prompt</span><span class="sh">"</span><span class="p">])</span>

<span class="n">training_data</span><span class="p">,</span> <span class="n">test_data</span> <span class="o">=</span> <span class="n">dataset</span><span class="p">[</span><span class="mi">0</span><span class="p">:</span><span class="mi">4</span><span class="p">],</span> <span class="n">dataset</span><span class="p">[</span><span class="mi">4</span><span class="p">:]</span>

<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Size of training data: </span><span class="si">{</span><span class="nf">len</span><span class="p">(</span><span class="n">training_data</span><span class="p">)</span><span class="si">}</span><span class="s">nSize of test data: </span><span class="si">{</span><span class="nf">len</span><span class="p">(</span><span class="n">test_data</span><span class="p">)</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span></code></pre></figure> <ul> <li><strong>Upload the training and test data to S3</strong> –– Once the training and test sets are ready and formatted as a list of dictionaries, we upload them to S3 as JSON lines using the utility function below:</li> </ul> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="k">def</span> <span class="nf">write_jsonlines_to_s3</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">s3_path</span><span class="p">):</span>
<span class="sh">"""</span><span class="s">Writes list of dictionaries as a JSON lines file to S3</span><span class="sh">"""</span>

    <span class="n">json_string</span> <span class="o">=</span> <span class="sh">""</span>
    <span class="k">for</span> <span class="n">d</span> <span class="ow">in</span> <span class="n">data</span><span class="p">:</span>
        <span class="n">json_string</span> <span class="o">+=</span> <span class="n">json</span><span class="p">.</span><span class="nf">dumps</span><span class="p">(</span><span class="n">d</span><span class="p">)</span> <span class="o">+</span> <span class="sh">"</span><span class="s">n</span><span class="sh">"</span>

    <span class="n">s3_client</span>   <span class="o">=</span> <span class="n">boto3</span><span class="p">.</span><span class="nf">client</span><span class="p">(</span><span class="sh">"</span><span class="s">s3</span><span class="sh">"</span><span class="p">)</span>

    <span class="n">bucket</span><span class="p">,</span> <span class="n">key</span> <span class="o">=</span> <span class="n">s3_utils</span><span class="p">.</span><span class="nf">parse_s3_url</span><span class="p">(</span><span class="n">s3_path</span><span class="p">)</span>
    <span class="n">s3_client</span><span class="p">.</span><span class="nf">put_object</span><span class="p">(</span>
        <span class="n">Body</span>   <span class="o">=</span> <span class="n">json_string</span><span class="p">,</span>
        <span class="n">Bucket</span> <span class="o">=</span> <span class="n">bucket</span><span class="p">,</span>
        <span class="n">Key</span>    <span class="o">=</span> <span class="n">key</span><span class="p">,</span>
    <span class="p">)</span>

<span class="nf">write_jsonlines_to_s3</span><span class="p">(</span><span class="n">training_data</span><span class="p">,</span> <span class="n">training_data_path</span><span class="p">)</span>
<span class="nf">write_jsonlines_to_s3</span><span class="p">(</span><span class="n">test_data</span><span class="p">,</span> <span class="n">test_data_path</span><span class="p">)</span></code></pre></figure> <h2 id="sagemaker-training-job">SageMaker Training job</h2> <p>With the datasets available in S3, we will now create a training job in Amazon SageMaker. For that, we have to create an entry point script, modify the configuration file specifying the training settings, and define an HuggingFace estimator. We will (re-)use the training script from <a href="https://github.com/mosaicml/llm-foundry">LLM Foundry</a> and <a href="https://github.com/mosaicml/composer">Composer</a> library’s CLI launcher that sets up the distributed training environment. Both of these packages are maintained by MosaicML, the company behind MPT-7B. The working folder should be structured like:</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="err">└──</span> <span class="n">fine</span><span class="o">-</span><span class="n">tune</span><span class="o">-</span><span class="n">mpt</span><span class="o">-</span><span class="mi">7</span><span class="n">b</span><span class="o">-</span><span class="n">sagemaker</span><span class="o">/</span>
<span class="err">├──</span> <span class="n">training_script_launcher</span><span class="p">.</span><span class="n">sh</span>
<span class="err">├──</span> <span class="n">fine_tuning_config</span><span class="p">.</span><span class="n">yaml</span>
<span class="err">├──</span> <span class="n">sagemaker_finetuning</span><span class="p">.</span><span class="n">ipynb</span></code></pre></figure> <p>We will now dive deep into each of these files.</p> <ul> <li><strong>Create a configuration file <a href="https://github.com/jpcpereira/sagemaker-fine-tune-mpt-7b/blob/main/finetuning_config.yaml"><code class="language-plaintext highlighter-rouge">finetuning_config.yaml</code></a></strong> –– The template provided in the <a href="https://github.com/mosaicml/llm-foundry">LLM Foundry</a> repository is a good starting point, specifically the <a href="https://github.com/mosaicml/llm-foundry/blob/main/scripts/train/yamls/finetune/mpt-7b_dolly_sft.yaml"><code class="language-plaintext highlighter-rouge">mpt-7b-dolly-sft.yaml</code></a> file. However, depending on your dataset size and training instance, you might have to adjust some of these configurations, such as the batch size. I have modified the file to fine-tune the model in SageMaker (check <a href="https://github.com/jpcpereira/sagemaker-fine-tune-mpt-7b/blob/main/finetuning_config.yaml"><code class="language-plaintext highlighter-rouge">finetuning_config.yaml</code></a>). The parameters that you should pay attention to are the following:</li> </ul> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="n">max_seq_len</span><span class="p">:</span> <span class="mi">512</span>
<span class="n">global_seed</span><span class="p">:</span> <span class="mi">17</span>

<span class="bp">...</span>

<span class="c1"># Dataloaders
</span>
<span class="n">train_loader</span><span class="p">:</span>
<span class="n">name</span><span class="p">:</span> <span class="n">finetuning</span>
<span class="n">dataset</span><span class="p">:</span>
<span class="n">hf_name</span><span class="p">:</span> <span class="n">json</span>
<span class="n">hf_kwargs</span><span class="p">:</span>
<span class="n">data_dir</span><span class="p">:</span> <span class="o">/</span><span class="n">opt</span><span class="o">/</span><span class="n">ml</span><span class="o">/</span><span class="nb">input</span><span class="o">/</span><span class="n">data</span><span class="o">/</span><span class="n">train</span><span class="o">/</span>
<span class="bp">...</span>

<span class="n">eval_loader</span><span class="p">:</span>
<span class="n">name</span><span class="p">:</span> <span class="n">finetuning</span>
<span class="n">dataset</span><span class="p">:</span>
<span class="n">hf_name</span><span class="p">:</span> <span class="n">json</span>
<span class="n">hf_kwargs</span><span class="p">:</span>
<span class="n">data_dir</span><span class="p">:</span> <span class="o">/</span><span class="n">opt</span><span class="o">/</span><span class="n">ml</span><span class="o">/</span><span class="nb">input</span><span class="o">/</span><span class="n">data</span><span class="o">/</span><span class="n">test</span><span class="o">/</span>

<span class="bp">...</span>
<span class="n">max_duration</span><span class="p">:</span> <span class="mi">3</span><span class="n">ep</span>
<span class="n">eval_interval</span><span class="p">:</span> <span class="mi">1</span><span class="n">ep</span>
<span class="bp">...</span>
<span class="n">global_train_batch_size</span><span class="p">:</span> <span class="mi">128</span>

<span class="bp">...</span>

<span class="c1"># FSDP
</span>
<span class="n">fsdp_config</span><span class="p">:</span>
<span class="n">sharding_strategy</span><span class="p">:</span> <span class="n">FULL_SHARD</span>
<span class="n">mixed_precision</span><span class="p">:</span> <span class="n">PURE</span>
<span class="n">activation_checkpointing</span><span class="p">:</span> <span class="n">true</span>
<span class="n">activation_checkpointing_reentrant</span><span class="p">:</span> <span class="n">false</span>
<span class="n">activation_cpu_offload</span><span class="p">:</span> <span class="n">false</span>
<span class="n">limit_all_gathers</span><span class="p">:</span> <span class="n">true</span>
<span class="n">verbose</span><span class="p">:</span> <span class="n">false</span>

<span class="c1"># Checkpoint to local filesystem or remote object store
</span>
<span class="n">save_folder</span><span class="p">:</span> <span class="o">/</span><span class="n">tmp</span><span class="o">/</span><span class="n">checkpoints</span>
<span class="n">dist_timeout</span><span class="p">:</span> <span class="mi">2000</span></code></pre></figure> <p>The <code class="language-plaintext highlighter-rouge">max_seq_length</code> indicates the maximum number of tokens of the input (remember that 100 tokens ~ 75 words). The training and test data will be loaded using the 😊 <a href="https://huggingface.co/docs/datasets/index">Datasets</a> library from the <code class="language-plaintext highlighter-rouge">/opt/ml/input/data/{train, test}</code> directory inside the container associated with the training job. Check out the <a href="https://docs.aws.amazon.com/sagemaker/latest/dg/model-train-storage.html">SageMaker Training Storage Folders</a>‘ documentation to understand how the container directories are structured. The <code class="language-plaintext highlighter-rouge">max_duration</code> specifies the number of epochs for fine-tuning. Two to three epochs is typically a good choice. <code class="language-plaintext highlighter-rouge">eval_interval</code> indicates how often the model will be evaluated on the test set.</p> <p>The distributed training strategy is Fully Sharded Data Parallel (FSDP), which enables efficient training of large models like MPT-7B. Unlike the traditional data parallel strategy, which keeps a copy of the model in each GPU, FSDP shards model parameters, optimizer states, and gradients across data parallel workers. If you want to learn more about FSDP, check this insightful <a href="https://pytorch.org/blog/introducing-pytorch-fully-sharded-data-parallel-api">PyTorch intro post</a>. FSDP is integrated in <a href="https://github.com/mosaicml/composer">Composer</a>, the distributed training library used by <a href="https://github.com/mosaicml/llm-foundry">LLM Foundry</a>.</p> <p><code class="language-plaintext highlighter-rouge">save_folder</code> determines where the model checkpoint (<code class="language-plaintext highlighter-rouge">.pt</code> file) is saved. We set it to the temporary folder <code class="language-plaintext highlighter-rouge">/tmp/checkpoints</code>.</p> <ul> <li><strong>Create the entry point script <a href="https://github.com/jpcpereira/sagemaker-fine-tune-mpt-7b/blob/main/launcher.sh"><code class="language-plaintext highlighter-rouge">launcher.sh</code></a></strong> –— A bash script is used as entry point. The bash script clones the LLM Foundry repository, installs requirements, and, more importantly, runs the training script using Composer library’s distributed launcher. Note that, typically, training jobs in SageMaker run the training script using a command like <code class="language-plaintext highlighter-rouge">python train.py</code>. However, it is possible to pass a bash script as entry point, which provides more flexibility in our scenario. Finally, we convert the model checkpoint saved to <code class="language-plaintext highlighter-rouge">/tmp/checkpoints</code> to the HuggingFace model format and save the final artifacts into <code class="language-plaintext highlighter-rouge">/opt/ml/model/</code>. SageMaker will compress all files in this directory, create a tarball <code class="language-plaintext highlighter-rouge">model.tar.gz</code>, and upload it to S3. The tarball is useful for inference.</li> </ul> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="c1"># Clone llm-foundry package from MosaicML
</span>
<span class="c1"># This is where the training script is hosted
</span>
<span class="n">git</span> <span class="n">clone</span> <span class="n">https</span><span class="p">:</span><span class="o">//</span><span class="n">github</span><span class="p">.</span><span class="n">com</span><span class="o">/</span><span class="n">mosaicml</span><span class="o">/</span><span class="n">llm</span><span class="o">-</span><span class="n">foundry</span><span class="p">.</span><span class="n">git</span>
<span class="n">cd</span> <span class="n">llm</span><span class="o">-</span><span class="n">foundry</span>

<span class="c1"># Install required packages
</span>
<span class="n">pip</span> <span class="n">install</span> <span class="o">-</span><span class="n">e</span> <span class="sh">"</span><span class="s">.[gpu]</span><span class="sh">"</span>
<span class="n">pip</span> <span class="n">install</span> <span class="n">git</span><span class="o">+</span><span class="n">https</span><span class="p">:</span><span class="o">//</span><span class="n">github</span><span class="p">.</span><span class="n">com</span><span class="o">/</span><span class="n">mosaicml</span><span class="o">/</span><span class="n">composer</span><span class="p">.</span><span class="n">git</span><span class="nd">@dev</span>

<span class="c1"># Run training script with fine-tuning configuration
</span>
<span class="n">composer</span> <span class="n">scripts</span><span class="o">/</span><span class="n">train</span><span class="o">/</span><span class="n">train</span><span class="p">.</span><span class="n">py</span> <span class="o">/</span><span class="n">opt</span><span class="o">/</span><span class="n">ml</span><span class="o">/</span><span class="n">code</span><span class="o">/</span><span class="n">finetuning_config</span><span class="p">.</span><span class="n">yaml</span>

<span class="c1"># Convert Composer checkpoint to HuggingFace model format
</span>
<span class="n">python</span> <span class="n">scripts</span><span class="o">/</span><span class="n">inference</span><span class="o">/</span><span class="n">convert_composer_to_hf</span><span class="p">.</span><span class="n">py</span>
<span class="o">--</span><span class="n">composer_path</span> <span class="o">/</span><span class="n">tmp</span><span class="o">/</span><span class="n">checkpoints</span><span class="o">/</span><span class="n">latest</span><span class="o">-</span><span class="n">rank0</span><span class="p">.</span><span class="n">pt</span>
<span class="o">--</span><span class="n">hf_output_path</span> <span class="o">/</span><span class="n">opt</span><span class="o">/</span><span class="n">ml</span><span class="o">/</span><span class="n">model</span><span class="o">/</span><span class="n">hf_fine_tuned_model</span>
<span class="o">--</span><span class="n">output_precision</span> <span class="n">bf16</span>

<span class="c1"># Print content of the model artifact directory
</span>
<span class="n">ls</span> <span class="o">/</span><span class="n">opt</span><span class="o">/</span><span class="n">ml</span><span class="o">/</span><span class="n">model</span><span class="o">/</span></code></pre></figure> <ul> <li><strong>Define 😊 HuggingFace Estimator</strong> –– The Estimator sets the Docker container used to run the training job. We will use an image with PyTorch 2.0.0 and Python 3.10. The bash script and the configuration file are automatically uploaded to S3 and made available inside the container (handled by the SageMaker Python SDK). We set the training instance to <a href="https://aws.amazon.com/ec2/instance-types/g5"><code class="language-plaintext highlighter-rouge">g5.48xlarge</code></a> that has 8x NVIDIA A10G GPUs. The <a href="https://aws.amazon.com/ec2/instance-types/p4"><code class="language-plaintext highlighter-rouge">p4d.24xlarge</code></a> is also a good choice. Even though it is more expensive, it is equipped with 8x NVIDIA A100 GPUs. We also indicate the metrics to track on the training and test sets (Cross Entropy and Perplexity). The values of these metrics are captured via Regex expressions and sent to Amazon CloudWatch.</li> </ul> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="c1"># Define container image for the training job
</span>
<span class="n">training_image_uri</span> <span class="o">=</span> <span class="sa">f</span><span class="sh">"</span><span class="s">763104351884.dkr.ecr.</span><span class="si">{</span><span class="n">region</span><span class="si">}</span><span class="s">.amazonaws.com/huggingface-pytorch-training:2.0.0-transformers4.28.1-gpu-py310-cu118-ubuntu20.04-v1.1</span><span class="sh">"</span>

<span class="c1"># Define metrics to send to CloudWatch
</span>
<span class="n">metrics</span> <span class="o">=</span> <span class="p">[</span> <span class="c1"># On training set
</span><span class="p">{</span><span class="sh">"</span><span class="s">Name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">train:LanguageCrossEntropy</span><span class="sh">"</span><span class="p">,</span>
<span class="sh">"</span><span class="s">Regex</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Train metrics/train/LanguageCrossEntropy: ([+-]?((d+.?d*)|(.d+)))</span><span class="sh">"</span><span class="p">},</span>
<span class="p">{</span><span class="sh">"</span><span class="s">Name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">train:LanguagePerplexity</span><span class="sh">"</span><span class="p">,</span>
<span class="sh">"</span><span class="s">Regex</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Train metrics/train/LanguagePerplexity: ([+-]?((d+.?d*)|(.d+)))</span><span class="sh">"</span><span class="p">},</span> <span class="c1"># On test set
</span><span class="p">{</span><span class="sh">"</span><span class="s">Name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">test:LanguageCrossEntropy</span><span class="sh">"</span><span class="p">,</span>
<span class="sh">"</span><span class="s">Regex</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Eval metrics/eval/LanguageCrossEntropy: ([+-]?((d+.?d*)|(.d+)))</span><span class="sh">"</span><span class="p">},</span>
<span class="p">{</span><span class="sh">"</span><span class="s">Name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">test:LanguagePerplexity</span><span class="sh">"</span><span class="p">,</span>
<span class="sh">"</span><span class="s">Regex</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Eval metrics/eval/LanguagePerplexity: ([+-]?((d+.?d*)|(.d+)))</span><span class="sh">"</span><span class="p">},</span>
<span class="p">]</span>

<span class="n">estimator_args</span> <span class="o">=</span> <span class="p">{</span>
<span class="sh">"</span><span class="s">image_uri</span><span class="sh">"</span><span class="p">:</span> <span class="n">training_image_uri</span><span class="p">,</span> <span class="c1"># Training container image
</span><span class="sh">"</span><span class="s">entry_point</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">launcher.sh</span><span class="sh">"</span><span class="p">,</span> <span class="c1"># Launcher bash script
</span><span class="sh">"</span><span class="s">source_dir</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">.</span><span class="sh">"</span><span class="p">,</span> <span class="c1"># Directory with launcher script and configuration file
</span><span class="sh">"</span><span class="s">instance_type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">ml.g5.48xlarge</span><span class="sh">"</span><span class="p">,</span> <span class="c1"># Instance type
</span><span class="sh">"</span><span class="s">instance_count</span><span class="sh">"</span><span class="p">:</span> <span class="mi">1</span><span class="p">,</span> <span class="c1"># Number of training instances
</span><span class="sh">"</span><span class="s">base_job_name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">fine-tune-mpt-7b</span><span class="sh">"</span><span class="p">,</span> <span class="c1"># Prefix of the training job name
</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="n">role</span><span class="p">,</span> <span class="c1"># IAM role
</span><span class="sh">"</span><span class="s">volume_size</span><span class="sh">"</span><span class="p">:</span> <span class="mi">300</span><span class="p">,</span> <span class="c1"># Size of the EBS volume attached to the instance (GB)
</span><span class="sh">"</span><span class="s">py_version</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">py310</span><span class="sh">"</span><span class="p">,</span> <span class="c1"># Python version
</span><span class="sh">"</span><span class="s">metric_definitions</span><span class="sh">"</span><span class="p">:</span> <span class="n">metrics</span><span class="p">,</span> <span class="c1"># Metrics to track
</span><span class="sh">"</span><span class="s">output_path</span><span class="sh">"</span><span class="p">:</span> <span class="n">output_path</span><span class="p">,</span> <span class="c1"># S3 location where the model artifact will be uploaded
</span><span class="sh">"</span><span class="s">code_location</span><span class="sh">"</span><span class="p">:</span> <span class="n">code_location</span><span class="p">,</span> <span class="c1"># S3 location where the source code will be saved
</span><span class="sh">"</span><span class="s">disable_profiler</span><span class="sh">"</span><span class="p">:</span> <span class="bp">True</span><span class="p">,</span> <span class="c1"># Do not create profiler instance
</span><span class="sh">"</span><span class="s">keep_alive_period_in_seconds</span><span class="sh">"</span><span class="p">:</span> <span class="mi">240</span><span class="p">,</span> <span class="c1"># Enable Warm Pools while experimenting
</span><span class="p">}</span>

<span class="n">huggingface_estimator</span> <span class="o">=</span> <span class="nc">HuggingFace</span><span class="p">(</span>\<span class="o">*</span>\<span class="o">*</span><span class="n">estimator_args</span><span class="p">)</span></code></pre></figure> <blockquote> <p>⚠️ Make sure to request the respective quotas for SageMaker Training, along with <a href="https://docs.aws.amazon.com/sagemaker/latest/dg/train-warm-pools.html">Warm Pools</a>‘ quota in case you are making use of this cool feature. If you plan to run many jobs in SageMaker, take a look at <a href="https://aws.amazon.com/savingsplans/ml-pricing">SageMaker Saving Plans</a>.</p> </blockquote> <ul> <li><strong>Launch the training job 🚀</strong> –– We have all set to start the training job on Amazon SageMaker:</li> </ul> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="n">huggingface_estimator</span><span class="p">.</span><span class="nf">fit</span><span class="p">({</span>
<span class="sh">"</span><span class="s">train</span><span class="sh">"</span><span class="p">:</span> <span class="nc">TrainingInput</span><span class="p">(</span>
<span class="n">s3_data</span><span class="o">=</span><span class="n">training_data_path</span><span class="p">,</span>
<span class="n">content_type</span><span class="o">=</span><span class="sh">"</span><span class="s">application/jsonlines</span><span class="sh">"</span><span class="p">),</span>
<span class="sh">"</span><span class="s">test</span><span class="sh">"</span><span class="p">:</span> <span class="nc">TrainingInput</span><span class="p">(</span>
<span class="n">s3_data</span><span class="o">=</span><span class="n">test_data_path</span><span class="p">,</span>
<span class="n">content_type</span><span class="o">=</span><span class="sh">"</span><span class="s">application/jsonlines</span><span class="sh">"</span><span class="p">),</span>
<span class="p">},</span> <span class="n">wait</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span></code></pre></figure> <p>The training time will depend on the size of your dataset. With our dummy dataset, training takes roughly <strong>20min</strong> to complete. Once the model is trained and converted to 😊 HuggingFace format, SageMaker will upload the model tarball (<code class="language-plaintext highlighter-rouge">model.tar.gz</code>) to the S3 <code class="language-plaintext highlighter-rouge">output_path</code>. I found that in practice the uploading step takes rather long (&gt;1h), which might be due to the size of the model artifacts to compress (~25GB).</p> <h2 id="summary">Summary</h2> <p>In this article, I showed how you can prepare a dataset and create a training job in SageMaker to fine-tune MPT-7B for your use case. The implementation leverages the training script from <a href="https://github.com/mosaicml/llm-foundry">LLM Foundry</a> and uses <a href="https://github.com/mosaicml/composer">Composer</a> library’s distributed training launcher. Once you have fine-tuned your model and want to deploy it, I recommend to check out the blog posts by <a href="https://www.philschmid.de">Philipp Schmid</a>; there are plenty of examples on how to deploy LLMs in SageMaker. Have fun with your fine-tuned MPT-7B model! 🎉</p> <p>All the code used in this article is available on my Github:</p> <blockquote> <p>GitHub – jpcpereira/sagemaker-fine-tune-mpt-7b</p> </blockquote>]]></content><author><name>João Pereira</name></author><category term="llm"/><category term="fine-tuning"/><category term="sagemaker"/><summary type="html"><![CDATA[Learn how to prepare a dataset and create a training job to fine-tune MPT-7B on Amazon SageMaker.]]></summary></entry><entry><title type="html">Fast and Scalable Hyperparameter Tuning and Cross-validation in AWS SageMaker</title><link href="https://www.jpcpereira.com/blog/2023/hyperparameter-tuning/" rel="alternate" type="text/html" title="Fast and Scalable Hyperparameter Tuning and Cross-validation in AWS SageMaker"/><published>2023-03-03T00:00:00+00:00</published><updated>2023-03-03T00:00:00+00:00</updated><id>https://www.jpcpereira.com/blog/2023/hyperparameter-tuning</id><content type="html" xml:base="https://www.jpcpereira.com/blog/2023/hyperparameter-tuning/"><![CDATA[<p>This article shares a recipe to <strong>speeding up to 60%</strong> your <strong>hyperparameter tuning with cross-validation in SageMaker Pipelines leveraging SageMaker Managed Warm Pools. By using Warm Pools, the runtime of a Tuning step with 120 sequential jobs is reduced</strong> from 10h to 4h.</p> <p>Improving and evaluating the performance of a machine learning model often requires a variety of ingredients. Hyperparameter tuning and cross-validation are 2 such ingredients. The first finds the best version of a model, while the second estimates how a model will generalize to unseen data. These steps, combined, introduce computing challenges as they require training and validating a model multiple times, in parallel and/or in sequence.</p> <p><strong>What this article is about…</strong></p> <ul> <li>What are Warm Pools and how to leverage them to speed-up hyperparameter tuning with cross-validation.</li> <li>How to design a production-grade SageMaker Pipeline that includes Processing, Tuning, Training, and Lambda steps.</li> </ul> <p>We will consider <a href="https://towardsdatascience.com/bayesian-optimization-for-hyperparameter-tuning-how-and-why-655b0ee0b399">Bayesian optimization</a> for hyperparameter tuning that leverages the scores of the hyperparameter combinations already tested to choose the hyperparameter set to test in the next round. We will use $ k $-fold cross-validation to score each combination of hyperparameters, in which the splits are as follows:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/folds-480.webp 480w,/assets/img/folds-800.webp 800w,/assets/img/folds-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/folds.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> $ k $-fold cross-validation strategy. </div> <p>The full dataset is partitioned into 𝑘 validation folds, the model trained on $ k-1 $ folds, and validated on its corresponding held-out fold. The overall score is the average over the individual validation scores obtained for each validation fold.</p> <hr/> <h2 id="what-are-warm-pools">What are Warm Pools?</h2> <p>Whenever a training job is launched in AWS, the provisioned instance takes roughly 3min to bootstrap before the training script is executed. This startup time adds up when running multiple jobs sequentially, which is the case when performing hyperparameter tuning using a Bayesian optimization strategy. Here, dozens or even hundreds of jobs are run in sequence leading to a significant total time that can be on par with or even higher than the actual execution times of the scripts.</p> <p><a href="https://aws.amazon.com/about-aws/whats-new/2022/09/reduce-ml-model-training-job-startup-time-8x-sagemaker-training-managed-warm-pools/">SageMaker Managed Warm Pools</a> make it possible to retain training infrastructure after a job is completed for a desired number of seconds, enabling saving the instance startup time for every subsequent job.</p> <p>Enabling Warm Pools is straightforward. You simply add an extra parameter (<code class="language-plaintext highlighter-rouge">keep_alive_period_in_seconds</code>) when creating a training job in SageMaker:</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="n">estimator</span> <span class="o">=</span> <span class="nc">Estimator</span><span class="p">(</span>
<span class="n">entry_point</span><span class="o">=</span><span class="sh">'</span><span class="s">training.py</span><span class="sh">'</span><span class="p">,</span>
<span class="n">keep_alive_period_in_seconds</span><span class="o">=</span><span class="mi">600</span><span class="p">,</span>
<span class="bp">...</span>
<span class="p">)</span></code></pre></figure> <p>If you want to learn more about SageMaker Managed Warm Pools, here is the documentation:</p> <blockquote> <p><a href="https://docs.aws.amazon.com/sagemaker/latest/dg/train-warm-pools.html">Train Using SageMaker Managed Warm Pools</a></p> </blockquote> <p>Now that we know what are Warm Pools, in Section <a href="#end-to-end-sagemaker-pipeline">End-to-End SageMaker Pipeline</a> we are going to dive deep into how to leverage them to speed-up the overall runtime of a SageMaker Pipeline that includes hyperparameter tuning with cross-validation.</p> <h2 id="end-to-end-sagemaker-pipeline">End-to-end SageMaker Pipeline</h2> <p>The following figure depicts an end-to-end SageMaker Pipeline that performs hyperparameter tuning with cross-validation.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/pipeline-480.webp 480w,/assets/img/pipeline-800.webp 800w,/assets/img/pipeline-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/pipeline.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Architecture diagram of the end-to-end SageMaker Pipeline. </div> <p>We will create the pipeline using the <a href="https://sagemaker.readthedocs.io/en/stable">SageMaker Python SDK</a>, which is an open-source library that simplifies the process of training, tuning, and deploying machine learning models in AWS SageMaker. The pipeline steps in the diagram are summarized as follows:</p> <ol> <li><strong>Data Preprocessing</strong> (<code class="language-plaintext highlighter-rouge">ProcessingStep</code>) — Data is retrieved from the source, transformed, and split into k cross-validation folds. An additional full dataset is saved for final training.</li> <li><strong>Hyperparameter Tuning With CV</strong> (<code class="language-plaintext highlighter-rouge">TuningStep</code>) — This is the step that we will concentrate on. It finds the combination of hyperparameters that achieves the best average performance across validation folds.</li> <li><strong>Optimal Hyperparameters Retrieval</strong> (<code class="language-plaintext highlighter-rouge">LambdaStep</code>) — Fires a Lambda function that retrieves the optimal set of hyperparameters by accessing the results of the hyperparameter tuning job using Boto3.</li> <li><strong>Final Training</strong> (<code class="language-plaintext highlighter-rouge">TrainingStep</code>) — Trains the model on the full dataset <code class="language-plaintext highlighter-rouge">train_full.csv</code> with the optimal hyperparameters.</li> <li><strong>Model Registration</strong> (<code class="language-plaintext highlighter-rouge">ModelStep</code>) –– Registers the final trained model in the SageMaker Model Registry.</li> <li><strong>Inference</strong> (<code class="language-plaintext highlighter-rouge">TransformStep</code>) –– Generates predictions using the registered model.</li> </ol> <p>Please find detailed documentation on how to implement these steps on the <a href="https://docs.aws.amazon.com/sagemaker/latest/dg/build-and-manage-steps.html">SageMaker Developer Guide</a>.</p> <h2 id="what-happens-inside-the-tuning-step">What happens inside the Tuning step?</h2> <p>Let’s now dig deeper into the <strong>pipeline step 2</strong> that iteratively tries and cross-validates multiple hyperparameter combinations in parallel and in sequence. The solution is represented in the following diagram:</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/hpo-480.webp 480w,/assets/img/hpo-800.webp 800w,/assets/img/hpo-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/hpo.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="eager" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Architecture diagram of the hyperparameter tuning with cross-validation step. </div> <p>The solution relies on SageMaker Automatic Model Tuning to create and orchestrate the training jobs that test multiple hyperparameter combinations. The Automatic Model Tuning job can be launched using the HyperparameterTuner available in the <a href="https://sagemaker.readthedocs.io/en/stable/">SageMaker Python SDK</a>. It creates $ M \times N $ hyperparameter tuning training jobs, $ M $ of which are run in parallel over $ N $ sequential rounds that progressively search for the best hyperparameters. Each of these jobs launches and monitors a set of $ K $ cross-validation jobs. At each tuning round, $ M \times K $ instances in a Warm Pool are <strong>retained for the next round</strong>. In the subsequent rounds there is no instance startup time.</p> <p>SageMaker’s <code class="language-plaintext highlighter-rouge">HyperparameterTuner</code> already makes use of Warm Pools as announced on the <a href="https://aws.amazon.com/about-aws/whats-new/2022/08/amazon-sagemaker-automatic-model-tuning-reuses-sagemaker-training-instances-reduce-start-up-overheads/">AWS News Blog</a>. However, the cross-validation training jobs that are created in each tuning job – that cross-validate a specific combination of hyperparameters – have to be <strong>manually created and monitored, and the provisioned instances are not kept in a Warm Pool</strong>. Each hyperparameter tuning training job will only finish when all the underlying cross-validation training jobs have completed.</p> <p>To bring the architecture above to life and enable Warm Pools for <strong>all</strong> training jobs, we need to create three main scripts: <code class="language-plaintext highlighter-rouge">pipeline.py</code>, <code class="language-plaintext highlighter-rouge">cross_validation.py</code>, and <code class="language-plaintext highlighter-rouge">training.py</code>:</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">pipeline.py</code> script</strong> –– Defines the SageMaker Pipeline steps described in Section <a href="#end-to-end-sagemaker-pipeline">End-to-End SageMaker Pipeline</a>, which includes SageMaker’s <code class="language-plaintext highlighter-rouge">HyperparameterTuner</code>:</li> </ul> <figure class="highlight"><pre><code class="language-python" data-lang="python">  <span class="c1">#pipeline.py script
</span>  <span class="bp">...</span>

  <span class="c1"># Steps 2 to 5
</span>
  <span class="n">tuner</span> <span class="o">=</span> <span class="nc">HyperparameterTuner</span><span class="p">(</span>
  <span class="n">estimator</span><span class="o">=</span><span class="n">estimator</span><span class="p">,</span>
  <span class="n">metric_definitions</span><span class="o">=</span><span class="p">[</span>
  <span class="p">{</span>
  <span class="sh">"</span><span class="s">Name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">training:score</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">Regex</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">average model training score:(.*?);</span><span class="sh">"</span>
  <span class="p">},</span>
  <span class="p">{</span>
  <span class="sh">"</span><span class="s">Name</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">validation:score</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">Regex</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">average model validation score:(.*?);</span><span class="sh">"</span>
  <span class="p">}</span>
  <span class="p">],</span>
  <span class="n">objective_metric_name</span><span class="o">=</span><span class="sh">"</span><span class="s">validation:score</span><span class="sh">"</span><span class="p">,</span>
  <span class="n">strategy</span><span class="o">=</span><span class="sh">"</span><span class="s">Bayesian</span><span class="sh">"</span><span class="p">,</span>
  <span class="n">max_jobs</span><span class="o">=</span><span class="n">max_jobs</span><span class="p">,</span> <span class="c1"># M x N
</span>  <span class="n">max_parallel_jobs</span><span class="o">=</span><span class="n">max_parallel_jobs</span> <span class="c1"># M
</span>  <span class="p">)</span>

  <span class="c1"># Step 2 - Hyperparameter tuning With cross-validation step
</span>
  <span class="n">step_tune</span> <span class="o">=</span> <span class="nc">TuningStep</span><span class="p">(</span>
  <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">tuning-step</span><span class="sh">"</span><span class="p">,</span>
  <span class="n">step_args</span><span class="o">=</span><span class="n">tuner</span><span class="p">.</span><span class="nf">fit</span><span class="p">({</span>
  <span class="sh">"</span><span class="s">train</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">&lt;s3-path-to-training-folds&gt;</span><span class="sh">"</span><span class="p">,</span>
  <span class="sh">"</span><span class="s">validation</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">&lt;s3-path-to-validation-folds&gt;</span><span class="sh">"</span>
  <span class="p">})</span>
  <span class="p">)</span>

  <span class="c1"># Step 3 - Optimal hyperparameter retrieval step
</span>
  <span class="n">step_lambda</span> <span class="o">=</span> <span class="nc">LambdaStep</span><span class="p">(</span>
  <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">get-optimal-hyperparameters-step</span><span class="sh">"</span><span class="p">,</span>
  <span class="n">lambda_func</span><span class="o">=</span><span class="n">lambda_get_optimal_hyperparameters</span><span class="p">,</span>
  <span class="n">inputs</span><span class="o">=</span><span class="p">{</span>
  <span class="sh">"</span><span class="s">best_training_job_name</span><span class="sh">"</span><span class="p">:</span> <span class="n">step_tune</span><span class="p">.</span><span class="n">properties</span><span class="p">.</span><span class="n">BestTrainingJob</span><span class="p">.</span><span class="n">TrainingJobName</span><span class="p">,</span>
  <span class="p">},</span>
  <span class="n">outputs</span><span class="o">=</span><span class="p">[</span>
  <span class="nc">LambdaOutput</span><span class="p">(</span><span class="n">output_name</span><span class="o">=</span><span class="sh">"</span><span class="s">hyperparameter_a</span><span class="sh">"</span><span class="p">),</span>
  <span class="nc">LambdaOutput</span><span class="p">(</span><span class="n">output_name</span><span class="o">=</span><span class="sh">"</span><span class="s">hyperparameter_b</span><span class="sh">"</span><span class="p">),</span>
  <span class="nc">LambdaOutput</span><span class="p">(</span><span class="n">output_name</span><span class="o">=</span><span class="sh">"</span><span class="s">hyperparameter_c</span><span class="sh">"</span><span class="p">)</span>
  <span class="p">]</span>
  <span class="p">)</span>

  <span class="c1"># Step 4 - Final training step
</span>
  <span class="n">step_train</span> <span class="o">=</span> <span class="nc">TrainingStep</span><span class="p">(</span>
  <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">final-training-step</span><span class="sh">"</span><span class="p">,</span>
  <span class="n">step_args</span><span class="o">=</span><span class="n">estimator</span><span class="p">.</span><span class="nf">fit</span><span class="p">({</span><span class="sh">"</span><span class="s">train</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">&lt;s3-path-to-full-training-set&gt;</span><span class="sh">"</span><span class="p">})</span>
  <span class="p">)</span>

  <span class="n">model</span> <span class="o">=</span> <span class="nc">Model</span><span class="p">(</span>
  <span class="n">model_data</span><span class="o">=</span><span class="n">step_train</span><span class="p">.</span><span class="n">properties</span><span class="p">.</span><span class="n">ModelArtifacts</span><span class="p">.</span><span class="n">S3ModelArtifacts</span><span class="p">,</span>
  <span class="bp">...</span>
  <span class="p">)</span>

  <span class="c1"># Step 5 - Model registration step
</span>
  <span class="n">step_model_registration</span> <span class="o">=</span> <span class="nc">ModelStep</span><span class="p">(</span>
  <span class="n">name</span><span class="o">=</span><span class="sh">"</span><span class="s">model-registration-step</span><span class="sh">"</span><span class="p">,</span>
  <span class="n">step_args</span><span class="o">=</span><span class="n">model</span><span class="p">.</span><span class="nf">register</span><span class="p">(.)</span>
  <span class="p">)</span>
  </code></pre></figure> <ul> <li><strong><code class="language-plaintext highlighter-rouge">cross_validation.py</code> script</strong> –– Serves as entry point of SageMaker’s <code class="language-plaintext highlighter-rouge">HyperparameterTuner</code>. It launches multiple cross-validation training jobs. It is inside this script that the <code class="language-plaintext highlighter-rouge">keep_alive_period_in_seconds</code> parameter has to be specified, when calling the SageMaker Training Job API. The script computes and logs the average validation score across all validation folds. Logging the value enables easy reading of that metric using Regex by the <code class="language-plaintext highlighter-rouge">HyperparameterTuner</code> (as in the code snippet above). This metric is going to be tagged to each combination of hyperparameters.</li> </ul> <blockquote> <p><strong>Tip:</strong> Add a small delay, i.e., a few seconds, between the calls to the SageMaker APIs that create and monitor the training jobs to prevent the “Rate Exceeded” error, as in the example:</p> </blockquote> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="c1">#cross_validation.py script
</span>
<span class="kn">import</span> <span class="n">time</span>
<span class="bp">...</span>

<span class="n">training_jobs</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">for</span> <span class="n">fold_index</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">number_of_folds</span><span class="p">):</span>

      <span class="c1"># Create cross-validation training jobs (one per fold)
</span>      <span class="n">job</span> <span class="o">=</span> <span class="nf">train_model</span><span class="p">(</span>
          <span class="n">training_data</span><span class="o">=</span><span class="sh">"</span><span class="s">&lt;training-data-s3-path&gt;</span><span class="sh">"</span>
          <span class="n">validation_data</span><span class="o">=</span><span class="sh">"</span><span class="s">&lt;validation-data-s3-path&gt;</span><span class="sh">"</span>
          <span class="n">fold_index</span><span class="o">=</span><span class="n">fold_index</span><span class="p">,</span>
          <span class="n">hyperparameters</span><span class="o">=</span><span class="p">{</span>
              <span class="sh">"</span><span class="s">hyperparameter_a</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">&lt;value-of-hyperparameter-a&gt;</span><span class="sh">"</span><span class="p">,</span>
              <span class="sh">"</span><span class="s">hyperparameter_b</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">&lt;value-of-hyperparameter-b&gt;</span><span class="sh">"</span><span class="p">,</span>
              <span class="sh">"</span><span class="s">hyperparameter_c</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">&lt;value-of-hyperparameter-c&gt;</span><span class="sh">"</span>
      <span class="p">})</span>
      <span class="n">training_jobs</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">job</span><span class="p">)</span>

      <span class="c1"># Add delay to prevent Rate Exceeded error.
</span>      <span class="n">time</span><span class="p">.</span><span class="nf">sleep</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span>

<span class="bp">...</span></code></pre></figure> <blockquote> <p><strong>Tip:</strong> Disable the <a href="https://docs.aws.amazon.com/sagemaker/latest/dg/debugger-turn-off.html">debugger profiler</a> when launching your SageMaker training jobs. These profiler instances will be as many as the training instances and can make the overall cost increase significantly. You can do so by simply setting <code class="language-plaintext highlighter-rouge">disable_profiler=True</code> in the Estimator definition.</p> </blockquote> <ul> <li><strong><code class="language-plaintext highlighter-rouge">training.py</code> script</strong> –– Trains a model on a given input training set. The hyperparameters being cross-validated are passed as arguments of this script.</li> </ul> <blockquote> <p><strong>Tip:</strong> Write a general-purpose <code class="language-plaintext highlighter-rouge">training.py</code> script and reuse it for training the model on cross-validation sets and for training the final model with the optimal hyperparameters on the full training set.</p> </blockquote> <p>To control each parallel cross-validation set of jobs, as well as to compute a final validation metric for each specific hyperparameter combination tested, there are several custom functions that have to be implemented inside the <code class="language-plaintext highlighter-rouge">cross_validation.py</code> script. <a href="https://github.com/aws-samples/sagemaker-cross-validation-pipeline">This example</a> provides good inspiration, even though it does not enable Warm Pools or Lambda.</p> <h3 id="how-many-jobs-are-created-in-total">How many jobs are created in total?</h3> <p>$ M \times N \times (K+1) $ jobs. Why?</p> <ul> <li>$ M \times N $ hyperparameter tuning training jobs – $ M $ in parallel and $ N $ in sequence – matches the number of hyperparameter combinations.</li> <li>$ K $ parallel cross-validation jobs per hyperparameter tuning training job + 1 (the hyperparameter tuning training job itself).</li> </ul> <p>If we have <strong>5</strong> validation folds, run <strong>4</strong> hyperparameter tuning training jobs in parallel and <strong>120</strong> in sequence, then the total <strong>number of jobs will be 2880</strong>.</p> <blockquote> <p><strong>Important:</strong> Make sure that you have all the required service quotas in place for the instance types that you are using. Check the AWS guides to understand how to set these quotas for both <a href="https://docs.aws.amazon.com/sagemaker/latest/dg/train-warm-pools.html#train-warm-pools-resource-limits">Warm Pools</a> and <a href="https://docs.aws.amazon.com/sagemaker/latest/dg/automatic-model-tuning-limits.html">Automatic Model Tuning</a>.</p> </blockquote> <h2 id="what-do-we-get-out-of-using-warm-pools">What do we get out of using Warm Pools?</h2> <p>Let’s say we want to run $ N=120 $ sequential training jobs and that the startup time of the instances is 3min and that training takes 2min to run (5min per job). This means that the total runtime is approximately:</p> <ul> <li><em>Without</em> Warm Pools: 5min x 120 jobs = <strong>10h</strong></li> <li><em>With</em> Warm Pools: 5min x 1 job + 2min x 119 jobs ≈ <strong>4h</strong></li> </ul> <p><strong>This means that with Warm Pools the process takes 60% less time!</strong></p> <h2 id="summary">Summary</h2> <p>In this article, I showed how we can leverage Warm Pools to significantly speed-up hyperparameter tuning with cross-validation in SageMaker Pipelines. Warm Pools are a great feature of SageMaker that not only enables more efficient production pipelines, but also faster iterations in experiments. At the moment, SageMaker Managed Warm Pools have been integrated in SageMaker Training, but not in SageMaker Processing.</p> <p><em>All images unless otherwise noted are by the author.</em></p>]]></content><author><name>João Pereira</name></author><category term="hyperparameter-tuning"/><category term="sagemaker"/><category term="aws"/><summary type="html"><![CDATA[Using SageMaker Managed Warm Pools.]]></summary></entry><entry><title type="html">Introducing Solar Scan</title><link href="https://www.jpcpereira.com/blog/2019/solarscan/" rel="alternate" type="text/html" title="Introducing Solar Scan"/><published>2019-10-27T00:00:00+00:00</published><updated>2019-10-27T00:00:00+00:00</updated><id>https://www.jpcpereira.com/blog/2019/solarscan</id><content type="html" xml:base="https://www.jpcpereira.com/blog/2019/solarscan/"><![CDATA[<h2 id="introduction">Introduction</h2> <p>Solar energy has been growing rapidly in The Netherlands. However, its potential is still underexplored when compared to other renewable energy sources, and this fact makes it an interesting alternative to explore. Furthermore, solar is also easy to adopt at the residential level and is accessible to most of the population.</p> <blockquote> I'd put my money on the sun and solar energy. What a source of power! I hope we don't have to wait until oil and coal run out before we tackle that. -Thomas Edison </blockquote> <p>One key action for the promotion of residential solar photovoltaic (PV) energy is the assessment of the solar potential. Assessing solar potential allows identifying the best locations where solar panels can be installed. We introduce Solar Scan, a tool that will help in assessing the solar potential in The Netherlands. Solar Scan uses satellite imagery data and information extracted from the CBS database.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/solarscanpost/0-480.webp 480w,/assets/img/posts/solarscanpost/0-800.webp 800w,/assets/img/posts/solarscanpost/0-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/solarscanpost/0.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>Solar Scan is composed of several modules that work seamlessly and process the data, including an AI engine and a spatial processing pipeline.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/solarscanpost/1-480.webp 480w,/assets/img/posts/solarscanpost/1-800.webp 800w,/assets/img/posts/solarscanpost/1-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/solarscanpost/1.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <h2 id="ai-power">AI Power</h2> <p>Solar Scan is powered by an AI module whose output is used for solar potential assessment. This module uses a deep convolutional neural network (CNN) to segment the building’s rooftops from satellite images and estimates the useful area available for installing solar panels. The neural architecture applied for segmentation is U-Net, which achieves state-of-the-art performance in many complex image segmentation tasks. The model was trained in a supervised fashion on a dataset containing map tiles and the corresponding segmented images. We trained it from scratch on CoLab, using accelerated hardware (GPU), during 50 training epochs.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/solarscanpost/2-480.webp 480w,/assets/img/posts/solarscanpost/2-800.webp 800w,/assets/img/posts/solarscanpost/2-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/solarscanpost/2.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <h2 id="spatial-processing-pipeline">Spatial Processing Pipeline</h2> <p>The CBS data extracted, and the rooftop segmentation output, are displayed on a map integrated in a web application.</p> <p>To do so, we developed a spatial processing pipeline that consisted of the following steps:</p> <ol> <li>Built a grid of polygons covering the area to be represented and associated them with their center coordinates.</li> <li>Stored the CBS information and the segmentation results in a spatial database.</li> <li>Assigned the spatial data in the database to the corresponding polygons.</li> <li>Set a Geographical Information System (GIS) engine to process the spatial data.</li> <li>Rendered the spatial data on an interactive map.</li> </ol> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/solarscanpost/3-480.webp 480w,/assets/img/posts/solarscanpost/3-800.webp 800w,/assets/img/posts/solarscanpost/3-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/solarscanpost/3.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>With this spatial data processing pipeline, we aim to have a platform in place to investigate and assess the solar potential in different geographical areas. For this purpose, we thought about developing two elements: an interactive map and a solar information summary associated with the viewed area on the map. We represent the CBS data extracted on the map. In addition, we use the rooftop’s segmentation as a basis to compute metrics that provide insights on the solar potential in the viewed area.</p> <p>The map includes multiple layers, showing the solar information aggregated in hexagon shapes. Each hexagon contains one or more neighborhoods. The information regards the number of solar panels installed, the power currently generated by solar panels, and a detailed view of the potential rooftops.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/solarscanpost/4-480.webp 480w,/assets/img/posts/solarscanpost/4-800.webp 800w,/assets/img/posts/solarscanpost/4-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/solarscanpost/4.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>The CBS data extracted, and the rooftop segmentation output, are displayed on a map integrated in a web application.</p> <p>The segmentation of satellite images, performed using a deep learning model, allows us to estimate the rooftop area available, in square meters, for a given view area. Based on this estimation, we compute several solar metrics that provide valuable insights useful for solar potential assessment. Specifically, we compute the following solar metrics:</p> <ul> <li>Number of solar panels installed.</li> <li>Available area for installing solar panels.</li> <li>Potential energy generation.</li> <li>Money savings for one year of generation.</li> <li>Solar potential index.</li> </ul> <p>Solar Scan includes a representation of the rooftop’s segmentation on an interactive map, as well as the solar insights computed for the viewed area, shown on the right side.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/posts/solarscanpost/5-480.webp 480w,/assets/img/posts/solarscanpost/5-800.webp 800w,/assets/img/posts/solarscanpost/5-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/assets/img/posts/solarscanpost/5.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <p>We designed, implemented and deployed a solution that provides two benefits for our individual or professional target users. Solar Scan helps users to grasp the current situation on solar panels already installed across neighborhoods. In addition, they are able to investigate the solar energy potential in a specific neighborhood.</p> <p>Solar Scan includes a representation of the rooftop’s segmentation on an interactive map, as well as the solar insights computed for the viewed area, shown on the right side.</p> <p><strong>We wish you like Solar Scan as much as we do!</strong></p>]]></content><author><name>João Pereira</name></author><category term="solar-scan"/><summary type="html"><![CDATA[A solar potential assessment tool powered by AI.]]></summary></entry></feed>